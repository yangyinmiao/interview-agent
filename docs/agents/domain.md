# 领域文档

这是一个单上下文仓库。

## 开始探索前

- 阅读仓库根目录下的 `CONTEXT.md`。
- 阅读 `docs/adr/` 下与当前调查范围相关的 ADR。
- 如果上述任一路径不存在，直接继续，无需提示。

## 术语

使用 `CONTEXT.md` 中定义的领域术语。如果缺少所需概念，应记录这一缺口，不要悄然引入相互冲突的术语。

## ADR 冲突

如果结论或拟议改动与现有 ADR 冲突，应明确指出，而不是悄然覆盖既有决策。
